{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$f=([Console]::In.ReadToEnd()|ConvertFrom-Json).tool_input.file_path; if($f -match '\\.py$'){ruff format $f *>$null; Write-Output ('{\"systemMessage\":\"✅ Hook ruff : ' + $f + ' formaté avec succès\"}')}"
          }
        ]
      }
    ]
  }
}
